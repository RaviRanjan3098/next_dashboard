
import React from 'react';

const page = () => {

  return (
    <div>
      <h1>Auctions with SSR</h1>
      <p>This page demonstrates server-side rendering (SSR) with Next.js.</p>
      <p>Data fetching is done on the server side, and the page is rendered with the fetched data.</p>
    </div>
  );
};

export default page;
